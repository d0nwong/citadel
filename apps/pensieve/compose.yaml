services:
  pensieve:
    build: .
    ports:
      - "3778:3778"
    environment:
      WORKSPACE_DIR: /workspace
    volumes:
      # read-only: the sweep in argus owns every file here
      - ${WORKSPACE_DIR:-~/git/argus}:/workspace:ro
    restart: unless-stopped
